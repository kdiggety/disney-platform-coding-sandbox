package interview;

/**
 * Placeholder class so the project compiles immediately.
 * Replace or delete when you paste the interview code sample.
 */
public final class Placeholder {
  private Placeholder() {}

  public static String hello(String name) {
    if (name == null || name.isBlank()) return "hello";
    return "hello " + name.trim();
  }
}
